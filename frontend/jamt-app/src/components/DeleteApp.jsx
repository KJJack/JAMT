

export default function DeleteApp({ application }) {
    return(
        <div>
            <h4>Are you sure you want to <b>DELETE</b> your application to {application.company}</h4>
        </div>
    );
}